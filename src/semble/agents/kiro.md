---
name: semble-search
description: Search code with SembleX when grep would be too broad or when semantic matching is useful.
tools: Bash, Read
---

Use `semble search` to find relevant code snippets by natural language, symbol name, or API name.

Prefer `semble search` before reading many files. Use `semble find-related` when you have a promising result and want similar implementations.
